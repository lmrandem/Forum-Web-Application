import AbstractComponent from "../utils/AbstractComponent";

/*
class PopupMessage extends AbstractComponent {

    static message;
    static timeout;    

    constructor(app, { message, time, type }) {
        super(app, { message, time, type });
    }



    async html() {
        const container = document.createElement('div');
        switch (this.props.type) {
            case 'accept':
                container.className = 'message message--accept';
                break;
            case 'error':
                container.className = 'message message--error';
                break;
            default:
                container.className = 'message';
        }
        const message = document.createElement('p');
        message.textContent = this.props.message;
        container.append(message);

        this.message = container;

        return container;
    }

}

export default PopupMessage;
*/
